---
description: กฎเหล็กและมาตรฐานในการเขียนโค้ด Mellow Oven - ห้ามละเมิด Data Integrity และ Security
---

 1. 🏗️ Tech Stack & Architecture (Backend & Logic Focus)
### **Core Stack**
- **Runtime:** React (Vite) + TypeScript
- **State Management:** **Zustand** (Store-First Architecture)
- **Database:** Supabase (PostgreSQL)
- **Utility:** `date-fns` (Date handling), `decimal.js` (Money math - *recommended*)
### **Architectural Patterns**
1.  **Store-First Logic:**
    - ห้ามเขียน Logic คำนวณเงินหรือตัดสต็อกที่ซับซ้อนใน Component (`.tsx`)
    - ให้ย้าย Logic ไปไว้ใน **Zustand Actions** (`src/store/slices/*.ts`) เสมอ เพื่อให้เรียกใช้ได้จากทุกที่และ Test ง่าย
    - **ตัวอย่าง:** อย่าคำนวณ `totalPrice` หน้า UI ให้สร้าง Selector หรือ Action ใน Store

2.  **Service Layer Abstraction:**
    - การเรียก Supabase ที่ซับซ้อน (เช่น RPC, Transaction) ให้แยกเป็น Function ใน `src/lib/supabase.ts` หรือ `src/services/*.ts`
    - ห้ามเรียก `supabase.from('...').select()` ดิบๆ ใน Component ถ้า Logic นั้นใช้ซ้ำบ่อย
## 2. 📝 Coding Standards (Strict Mode)
### **Naming Convention**
- **Files/Components:** `PascalCase` (e.g., `InventoryCalculator.ts`, `FinanceService.ts`)
- **Functions/Variables:** `camelCase` (e.g., `calculateGrossProfit`, `fetchDailyInventory`)
- **Database Fields:** `snake_case` (e.g., `produced_qty`, `business_date`) -> ต้อง map เป็น `camelCase` ใน Frontend เสมอ
- **Constants:** `UPPER_SNAKE_CASE` (e.g., `MAX_RETRY_ATTEMPTS = 3`)
### **Type Safety**
- ❌ **NO `any`:** ห้ามใช้ `any` เด็ดขาด หากไม่แน่ใจให้ใช้ `unknown` แล้วทำ Type Guard
- ✅ **Interfaces:** ทุก Data Model ต้องมี Interface ชัดเจนใน `src/types.ts` หรือ `src/store/types.ts`
- ✅ **Strict Null Checks:** ต้องเช็ค `null` หรือ `undefined` เสมอ โดยเฉพาะข้อมูลจาก Database

### **Comments & Documentation**
- **JSDoc:** บังคับใช้กับ Function ที่มีความซับซ้อน (ระบุ `@param`, `@returns`, และ `@throws`)
- **Critical Logic:** ต้องมี Comment แปะหัวเสมอ เช่น `// 🛡️ Zombie Check: ป้องกันการส่งสินค้าที่ถูกลบ`
## 3. 🛡️ Security & Data Integrity (Priority P0)

### **1. RLS (Row Level Security)**
- ทุก Table ที่สร้างใหม่ **ต้องเปิด RLS เสมอ** (`ALTER TABLE x ENABLE ROW LEVEL SECURITY;`)
- ต้องเขียน Policy ให้ชัดเจน (เช่น `Authenticated users only`) ห้ามเปิด `public` read/write โดยไม่จำเป็น

### **2. Zombie & Ghost Data Prevention**
- **Loop Check:** ห้ามวนลูปอัปเดตข้อมูลโดยไม่เช็คว่า ID นั้นยังมีตัวตนใน Master Data หรือไม่
- **Pessimistic UI:** สำหรับการลบข้อมูลสำคัญ (เงิน/สต็อก) ให้รอ Server ตอบกลับ `200 OK` ก่อนค่อยลบออกจากหน้าจอ

### **3. Race Condition Handling**
- ห้ามใช้ Pattern: *Read -> Calculate in JS -> Write back* กับข้อมูลที่แชร์กัน (เช่น Stock)
- **ต้องใช้:** Database RPC (`increment_counter`), Atomic Update, หรือ Optimistic Locking
## 4. ⚡ Performance Guidelines

### **1. Batch Operations**
- ❌ **ห้าม:** ยิง API ใน Loop (`await supabase.insert(...)` ใน for-loop)
- ✅ **ต้อง:** รวบข้อมูลเป็น Array แล้วยิงครั้งเดียว (`supabase.rpc('bulk_upsert', { records })`)

### **2. Re-render Optimization**
- การเรียกใช้ Zustand Store ให้เลือกเฉพาะ State ที่ใช้จริง
  - ❌ `const { transactions } = useStore();` (จะ render ใหม่เมื่อ store เปลี่ยนแม้นิดเดียว)
  - ✅ `const transactions = useStore((state) => state.transactions);`
## 5. 🧪 Testing Strategy (Logic Only)

เนื่องจากเราเน้นความถูกต้องของข้อมูล ให้โฟกัสการเขียน Test ที่ **Logic**:
- **Unit Test (Vitest):**
    - เทสสูตรคำนวณเงิน (Finance Slices)
    - เทสการตัดสต็อก (Inventory Logic)
    - เทส Newsvendor Model (Forecasting)
- **Mocking:** ให้ Mock Supabase Client เพื่อเทส Logic โดยไม่ต้องยิง DB จริง
## 6. 🤖 AI Interaction Protocol

เมื่อได้รับคำสั่งจาก User:
1.  **Analyze:** วิเคราะห์ผลกระทบต่อ Database และ Logic เก่าก่อนเสมอ
2.  **Plan:** ถ้าเป็นการแก้ Logic สำคัญ (การเงิน/สต็อก) **ต้องเสนอแผนการแก้ก่อนลงมือทำ**
3.  **Execute:** เขียนโค้ดตามมาตรฐานข้างต้น
4.  **Verify:** ตรวจสอบตัวเองว่าลืม `await` หรือลืม Handle Error ไหม
## 7. 🐛 Advanced Debugging Protocol (ห้ามเดามั่ว)

เมื่อพบ Error หรือ Bug ห้ามใช้เทคนิค "Shotgun Debugging" (แก้สุ่มไปเรื่อยๆ เผื่อฟลุ๊ค) แต่ให้ทำตามขั้นตอน:

1.  **Trace the Data Flow:**
    * เริ่มไล่จาก `UI Input` -> `Zustand Action` -> `API Call` -> `Database RLS` -> `Database Table`.
    * หาจุดที่ข้อมูล "ขาดหาย" หรือ "ผิดเพี้ยน" ให้เจออย่างเจาะจง

2.  **Verify Assumptions:**
    * อย่าเชื่อ Error Message 100% (บางทีมันฟ้องผิดที่)
    * เช็ค `Network Tab` เสมอว่า Payload ที่ส่งไปหน้าตาเป็นยังไง?
    * เช็ค `Supabase Logs` ว่ามี Database Error หรือ RLS Violation หรือไม่?

3.  **Minimal Reproduction:**
    * ถ้าแก้ไม่ได้ ให้ลองตัดปัจจัยรบกวนออก (เช่น ลองยิง API เปล่าๆ โดยไม่ผ่าน UI) เพื่อแยกแยะว่าเป็นปัญหาที่ Frontend หรือ Backend

---

## 8. 🗄️ Database Evolution Strategy (Safe Migrations)

การแก้ไข Database คือจุดเสี่ยงสูงสุด ให้ใช้หลักการ **"Non-Destructive Changes"**:

1.  **Never Rename/Delete Immediately:**
    * ❌ ห้าม `DROP COLUMN` หรือ `RENAME COLUMN` ในทันทีถ้ามีข้อมูลอยู่
    * ✅ ให้สร้าง Column ใหม่ -> Migrate ข้อมูล -> แล้วค่อยลบของเก่าใน Patch ถัดไป

2.  **Data Preservation:**
    * ก่อนรันคำสั่ง `UPDATE` หรือ `DELETE` ล็อตใหญ่ ต้องเขียน `SELECT` มาเช็คก่อนว่ากระทบกี่แถว
    * ต้องมีแผนสำรอง (Backup Plan) เสมอ เช่น การ Export JSON ของ Table นั้นเก็บไว้ก่อน

3.  **Constraint First:**
    * พยายามใช้ Database Constraints (`NOT NULL`, `CHECK`, `UNIQUE`) คุมความถูกต้องของข้อมูล แทนที่จะพึ่งพาแค่ Frontend Logic

---

## 9. 🧹 The "Boy Scout" Refactoring Rule

**"Leave the code better than you found it."**
ทุกครั้งที่เข้าไปแก้ไฟล์ไหน ให้ทำความสะอาดไฟล์นั้นด้วยเสมอ:

1.  **Remove Dead Code:** ลบ imports ที่ไม่ได้ใช้, ตัวแปรที่ประกาศแล้วไม่ใช้, คอมเมนต์เก่าที่ไม่ update
2.  **Fix Types:** ถ้าเจอ `any` หลงเหลืออยู่ ให้แก้เป็น Type ที่ถูกต้องทันที
3.  **Extract Constants:** ถ้าเจอ Magic Number (เช่น `0.07`, `100`) ให้ย้ายไปเป็น `const` ที่มีชื่อสื่อความหมาย

---

## 10. 🧠 Cognitive Architecture (วิธีคิดเพื่อความเป็นเลิศ)

เพื่อให้งานออกมาดีที่สุด ให้ใช้หลักการคิดดังนี้:

### **1. First Principles Thinking (คิดจากรากฐาน)**
* อย่าแค่ Copy/Paste โค้ดเดิม
* ให้ถามตัวเองว่า *"จริงๆ แล้วฟีเจอร์นี้ต้องการแก้อะไร?"* และ *"วิธีนี้คือวิธีที่ Simple และ Performance ดีที่สุดแล้วหรือยัง?"*

### **2. Edge Case Obsession (หมกมุ่นกับเคสหลุด)**
* อย่าเขียนโค้ดแค่ "Happy Path" (ทางที่ราบรื่น)
* **ต้องถามตัวเองเสมอ:**
    * "ถ้าเน็ตหลุดตอนกดปุ่มนี้ ข้อมูลจะพังไหม?"
    * "ถ้า User เปิด 2 แท็บพร้อมกัน แล้วกดปุ่มเดียวกัน จะเกิด Race Condition ไหม?"
    * "ถ้าข้อมูลเป็น `null` หรือ `undefined` หน้าจอจะขาวไหม?"

### **3. Scalability Mindset**
* "โค้ดนี้จะยังทำงานได้ดีไหม ถ้าข้อมูลใน Table เพิ่มจาก 100 แถว เป็น 1,000,000 แถว?"
* ถ้าคำตอบคือ **"ไม่"** (เช่น การไม่มี Pagination หรือ Index) -> **ห้ามส่งงานนั้น**
## 11. ✅ The "Definition of Done" (Quality Gate)

ก่อนจะรายงานว่า "เสร็จแล้ว" (Task Complete) ต้องผ่าน Checklist นี้ทุกข้อ:

- [ ] **Type Check:** ไม่มี TypeScript Error แดงเถือกในไฟล์ที่แก้
- [ ] **Console Clean:** ไม่มี `console.log` ขยะหลงเหลือ (ยกเว้น `console.error` ที่จำเป็น)
- [ ] **Data Safety:** ยืนยันว่า Logic ไม่ทำให้ข้อมูลเก่าหาย หรือเกิด Zombie Data
- [ ] **Performance:** ไม่มีการ Re-render แบบไม่จำเป็น หรือ Loop API Request
- [ ] **Self-Review:** อ่านโค้ดตัวเองอีกรอบเหมือนเป็นคนอื่นมาตรวจ (Code Review Yourself)
## 12. 🕸️ Network Resilience (เน็ตหลุด ห้ามแอปฯ พัง)

1.  **Graceful Degradation:**
    * ถ้า Fetch API ล้มเหลว หน้าจอห้ามขาวโพลน (White Screen of Death)
    * ต้องมี **Skeleton Loading** หรือ **Retry Button** เสมอ
2.  **Timeout Handling:**
    * ห้ามปล่อยให้ User รอนานเกิน 10 วินาทีโดยไม่มีอะไรเกิดขึ้น ต้องมี Timeout Logic
3.  **Offline Awareness:**
    * (สำหรับ POS/Stock) ถ้าเน็ตหลุด ต้องแจ้งเตือน User ว่า "กำลังทำงานแบบออฟไลน์" และห้ามให้กดปุ่มที่เสี่ยงต่อข้อมูลหาย
## 13. 🧹 Memory Leak Prevention (เก็บกวาดขยะเสมอ)

1.  **Cleanup Function:**
    * ทุกครั้งที่ใช้ `useEffect` ที่มีการ Subscribe, setInterval, หรือ AddEventListener **ต้องมี return cleanup function เสมอ**
2.  **Abort Controller:**
    * หาก User เปลี่ยนหน้าขณะที่ API ยังโหลดไม่เสร็จ ต้องสั่ง `abort()` request นั้นทันที เพื่อประหยัด Resource
3.  **Zombie Listeners:**
    * เช็คเสมอว่าไม่มี Supabase Realtime Subscription ค้างอยู่ใน Background เมื่อ Component ถูก Unmount
## 14. 🛡️ Runtime Validation (ไม่เชื่อ TypeScript อย่างเดียว)

1.  **Response Validation:**
    * ข้อมูลที่ได้จาก API/Supabase **ห้ามเชื่อว่าตรง Type 100%**
    * ควรใช้การเช็คเบื้องต้น (เช่น `if (!data || !data.id) return;`) ก่อนนำไปใช้
2.  **User Input Sanitization:**
    * Input ที่เป็นตัวเลข (Money/Qty) ต้องผ่านการ `.replace(/[^0-9.]/g, '')` หรือเช็ค `isNaN` ก่อนส่งเข้า Logic คำนวณเสมอ
## 15. 🔁 Idempotency (กดเบิ้ล ต้องไม่พัง)

1.  **Disable Button on Submit:**
    * ทันทีที่กดปุ่ม "บันทึก/จ่ายเงิน" ปุ่มต้องเปลี่ยนสถานะเป็น `disabled` และขึ้น `Loading` ทันที (ห้าม User กดรัว)
2.  **Debounce/Throttle:**
    * Input Search หรือปุ่มที่ยิง API ถี่ๆ ต้องใส่ Debounce (รอหยุดพิมพ์ค่อยยิง) เสมอ
3.  **Unique Request ID:**
    * (Advanced) สำหรับ Transaction การเงิน ควรแนบ UUID ไปด้วย เพื่อให้ Server รู้ว่านี่คือรายการเดิม ไม่ใช่รายการใหม่
## 16. ⚡ Implicit Performance (สิ่งที่มองไม่เห็น)

1.  **Index Awareness:**
    * ทุกครั้งที่เขียนคำสั่ง `WHERE column = value` ให้ถามตัวเองว่า **"Column นี้ทำ Index ใน Database หรือยัง?"**
    * ถ้ายัง และตารางมีแนวโน้มจะใหญ่ -> **ต้องแจ้ง User ให้ทำ Index**
2.  **Memoization Strategy:**
    * Component ลูกที่รับ Props เป็น Object/Array/Function ต้องหุ้มด้วย `useMemo` หรือ `useCallback` เสมอ เพื่อป้องกันการ Re-render ไร้สาระ
## 17. ♿ Accessibility as Code Quality (โค้ดที่ทุกคนใช้ได้)

1.  **No Clickable Divs:** ห้ามใช้ `<div onClick={...}>` ให้ใช้ `<button>` เสมอ (เพื่อให้กด Tab หรือ Enter ได้)
2.  **Input Labels:** ทุก Input ต้องมี Label หรือ `aria-label`
3.  **Focus Management:** เมื่อเปิด Modal ต้อง Focus ไปที่ Input แรก และเมื่อปิด Modal ต้องคืน Focus กลับไปที่ปุ่มเดิม
## 18. 📦 Dependency Hygiene (ไม่ขี่ช้างจับตั๊กแตน)

1.  **No Bloatware:**
    * ห้าม import library ขนาดใหญ่มาเพื่อใช้แค่ฟังก์ชันเดียว (เช่น import ทั้ง `lodash` เพื่อใช้แค่ `_.get`)
    * ถ้าเขียนเองได้ใน 5-10 บรรทัด **ให้เขียนเอง** (Utils Function)
2.  **Clean Imports:**
    * จัดระเบียบ Import ให้แยกกลุ่ม (React, Libraries, Local Components, Utils, Types) เพื่อให้อ่านง่าย
## 19. 🔮 Future-Proofing (คิดเผื่ออนาคต)

1.  **No Magic Strings/Numbers:**
    * ห้ามเขียน `'CONFIRMED'` หรือ `0.07` ใน Logic ดิบๆ
    * ต้องแยกเป็น `const ORDER_STATUS = { CONFIRMED: 'CONFIRMED' }` หรือ `const VAT_RATE = 0.07`
2.  **Scalable UI:**
    * เมื่อออกแบบ Card หรือ List ให้คิดเผื่อเสมอว่า "ถ้าข้อความยาว 3 บรรทัด Layout จะแตกไหม?" (ใช้ `truncate` หรือ `line-clamp` กันไว้เสมอ)
## 20. 😈 The "Devil's Advocate" Self-Review (ขั้นตอนสุดท้าย)

ก่อนส่งงาน AI ต้องสวมบทบาทเป็น "Hacker" หรือ "QA จอมจับผิด" เพื่อตรวจงานตัวเอง:

1.  **"ถ้าฉันเป็น User แล้วเน็ตตัดตอนนี้ เงินจะหายไหม?"**
2.  **"ถ้าฉันกรอก Emoji ลงในช่องจำนวนเงิน ระบบจะ Crash ไหม?"**
3.  **"ถ้าฉันเปิดหน้านี้ทิ้งไว้ 24 ชั่วโมง แล้วกลับมาใช้ต่อ Token จะหมดอายุจน Error ไหม?"**

**หากตอบไม่ได้ข้อไหน ห้ามส่งโค้ดนั้นเด็ดขาด.**
## 21. 🧼 The "Sticky Zero" Hygiene (จัดการเลข 0 ให้อยู่หมัด)

1.  **Strip Leading Zeros:**
    * เมื่อ User พิมพ์ตัวเลข `05` ระบบต้องแปลงเป็น `5` ทันที (ยกเว้น `0.` สำหรับทศนิยม)
    * *Logic:* `value.replace(/^0+(?!\.)/, '')`
2.  **Allow Empty State:**
    * ห้ามบังคับให้ State เป็น `0` ทันทีที่ User ลบทุกอย่าง
    * ต้องยอมให้ State เป็น `""` (Empty String) ได้ชั่วคราว เพื่อให้ User พิมพ์เลขใหม่ได้สะดวก
    * ค่อยแปลงเป็น `0` ตอน `onBlur` (เมื่อกดออกจากช่อง) เท่านั้น
3.  **Decimal Handling:**
    * ถ้า User พิมพ์ `10.` (มีจุด) ห้ามรีบแปลงเป็น `10` (เพราะเขาอาจจะกำลังพิมพ์ `.5` ต่อ) ต้องรอให้พิมพ์เสร็จก่อน

## 22. 🖱️ Click-Area & Interaction Physics (ฟิสิกส์การกดที่สมจริง)

1.  **Fat Finger Friendly:**
    * ปุ่มกดในมือถือต้องมีความสูงอย่างน้อย `44px`
    * ถ้า Icon เล็ก ให้เพิ่ม `padding` รอบๆ หรือใช้ Wrapper เพื่อขยายพื้นที่กด (Clickable Area) ให้กว้างกว่าตัว Icon
2.  **Backdrop Dismissal:**
    * Modal/Popup ทุกตัว ต้องปิดได้เมื่อกดพื้นที่ว่างข้างหลัง (Backdrop Click)
    * ต้องปิดได้เมื่อกดปุ่ม `ESC` บนคีย์บอร์ด
3.  **Scroll Lock:**
    * เมื่อเปิด Modal ต้องสั่ง `document.body.style.overflow = 'hidden'` เสมอ เพื่อไม่ให้หน้าหลังเลื่อนไปมาจนเวียนหัว

---

